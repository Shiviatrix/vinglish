package std
module io

// LLVM libc bindings for I/O
foreign function printf(text format) returns number
foreign function puts(text str) returns number
foreign function putchar(number char) returns number

function print(text str) returns number
begin
    return puts(str)
end
